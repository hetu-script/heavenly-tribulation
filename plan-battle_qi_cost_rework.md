# 战斗费用系统重构计划：统一气费用模型

> 状态：实施中。Phase 0 / 1 已完成。
> **本轮范围**：代码、数据（战斗内）、文档。卡面 pip UI（万智牌式多图标费用显示）暂缓人工完成；**天赋树（passive_skills.json5）与卡牌构筑界面（card_library）本轮不动**，节点赋能与构筑面板为后续单独任务。
> 本文档吸收并取代 `plan-card_cost_pip.md`（2026-02）的费用模型与 UI 部分。

## 0. 美术资源现状（用户提供，2026-09-16）

- 新费用图标已就位：`assets/images/icon/cost/qi_{basic,mana,chakra,rage,karma,ultimate}.png`（依次对应元气、灵气、剑气、怒气、煞气、无极之气）。
- 死气图标：`assets/images/icon/status/decay.png`。
- 旧的 `energy_positive/negative_*.png` 状态图标已移除——status_effect.json5 的引用已在 Phase 1 修复（映射表见 §5）。
- 勾玉基础图（蓝色）已生成：`assets/images/icon/status/qi_magatama_base_v2.jpg`，其他颜色由用户编辑软件派生。
- 卡面 pip 组合的美术设计（无色徽章底座、pip 排列样式）**人工完成，暂缓**。

## 1. 背景与现状（2026-09 调研结论）

- **流派限制已不存在**：`lib/logic/logic.dart` 的 genre 检查已注释，现仅检查境界/装备/属性（Phase 0 已删除注释代码）。
- **资源不足无惩罚（旧行为）**：exhaust 词条资源不足时"全有或全无"，卡牌照常结算——Phase 2 改为费用硬检查。
- 现存顺带问题（Phase 0 已处理）：`enable_mana` 状态缺失已补齐（status_effect.json5 + status_script.ht）；怒气双重减半已删一处（enable_rage 回调），另一处随 Phase 3 新模型移除；`kResourceHasNegatives` 死代码已删。
- 遗留待查：`cultivation.dart:1689` 突破后 `rank = difficulty` 疑似应为 +1（不在本计划范围内）。

## 2. 已确认的设计决策

1. **取消卡牌流派限制**（追认现状）：仅保留境界/装备/属性限制。流派身份由软限制维持：资源不对称、节点产出加成（后续）、机制加成。
2. **费用硬检查**：资源不足的牌不能打出。exhaust 词条消耗转化为卡牌有色费用，两个改动合并落地。
3. **万智牌式费用模型**：卡牌总费用 = rank + 1，由无色部分（元气支付）+ 有色部分（四色气支付）组成。
4. **元气 = 无色费用**：每回合开始获得 rank + 3（现状不变，不累积）；回合结束按剩余量回血（见 4.1）作为空过补偿；图标保留至下个回合开始时清空（见决策 5）。
5. **资源气的生命周期 = 一整轮**：四色气、无极之气（以及元气）在**持有者的下个回合开始时清空**，而非自己回合结束时——即持有期间覆盖对方的整个回合。理由：
   - 怒气的"每层受伤 +5%"等防御侧效果，受伤发生在对方回合，必须让资源活到对方回合才有意义；
   - 对方能看到你没花完的费用（炉石式信息）；
   - 没花掉的怒气在对方回合是防御风险——花掉怒气既是进攻收益也是卸除防御 debuff。
   回合结束的结算（元气回血、溢出利用、煞气记录未用量）仍在自己回合结束按剩余量进行，仅清空时机延后。
6. **无极之气 = 万能色**：落地"可支付任意有色费用"，优先扣本色气、不足自动用无极补差；保留稀有度（rank 4 产气词缀）。
7. **煞气特殊化**：跨战斗永久积累池（karma）保留；天赋开通后每回合开始从 karma 池提取一部分作为当回合煞气；回合结束记录未用量，**下个回合开始清空时未使用的煞气返回 karma 池**（而非消失）。
8. **中立气转普通状态**：正气、豪气（→"幸运"）、清气、浩然之气（→"护盾"）取消资源属性，行为不变（Phase 1 已完成）。
9. **阴气精简为 6 种资源**：保留与元气、四色、无极对应的死气、逆灵之气、止戈之气、非战之气、无心之气、虚空之气；除现有负面效果外，统一附加"对应费用 +1/层"的增费作用（同色至多 +2）。戾气、衰气、浊气、萧索之气已转普通 debuff 状态（Phase 1 已完成）。
10. **阴气不入基础卡费用**：极少数绝世卡可例外（exotic 费用，如"需求 3 层死气"），保留绝世词条利用阴气的扩展空间。
11. **阴阳净值显示**：每种资源气只占一个 UI 槽位，显示净值（阳 − 阴），阴大于阳时同一图标用反色/深底区分（见第 5 节）。
12. **流派节点产出资源而非减费**：流派境界节点提供每回合额外的本流派颜色气（见第 7 节）。减费不再作为通用节点模板，仅保留为个别绝世卡/特殊节点的特殊效果。**节点数据的落地是后续任务，本轮不实施**（见 §7 注）。
13. **软狂暴机制与死气拆分**：新增非资源状态"劫气"（tribulation，Phase 1 已建条目与脚本），行为与现行软狂暴完全相同（每 16 回合双方叠层、回合开始消耗 1 层失去 10% 生命上限），但**不参与阴阳对冲、不影响费用**。死气回归纯粹的"元气对应阴气"定位，不再自动叠层（Phase 3 切换叠层代码）。

## 3. 费用数据模型

### 3.1 卡牌实例与 cards.json5

```
card.cost        // 无色费用（int），引擎渲染与现有代码继续用
card.costColored // 有色费用映射，如 { spell: 3 } 或 { unarmed: 2, curse: 1 }（法身双色）
                 // 无流派卡为 null 或 {}
// 不变量：cost + ΣcostColored.values = rank + 1（校验脚本保证）
```

字段命名用 `costColored`（不用 costChi，避免与真气 chi 伤害类型撞名）。

- cards.json5 主词条新增可选字段 `costColored`。
- **缺省推导**：有流派的卡按"有色 = ⌈(rank+1)/2⌉、无色 = ⌊(rank+1)/2⌋"分配，颜色由 genre→颜色映射决定；无流派卡全无色。逐卡微调时可参考该卡原 exhaust 消耗量填写显式 `costColored`。
- **显式填写优先**：用于特殊卡；校验总费用不变量，且基础卡的费用色必须属于 4 色（exotic 费用仅允许 `isUnique: true` 的卡）。加载期校验已在 Phase 1 落地（lib/data/game.dart `_validateBattleCardCostChi`，随字段改名同步为 costColored）。

### 3.2 genre → 费用色映射（常量，三处同步，Phase 1 已完成）

```
spellcraft → spell（灵气）
swordcraft → weapon（剑气）
bodyforge  → unarmed（怒气）
vitality   → curse（煞气）
avatar     → unarmed + curse（双色，对半拆；奇数时无色少 1）
```

已落地：`kGenreCostColors` / `kCostColorStatusIds` / `kWildcardStatusId`（lib/data/common.dart，并导出至 lib/data/constants.dart 与 scripts/main/binding/constants.ht）。

### 3.3 支付与检查规则

- **费用色集合由 costColored 的键空间天然限定**：合法键只有 spell / weapon / unarmed / curse（+ exotic 仅绝世卡），不需要额外的角色声明字段。
- **可打出检查**：`无色费用 ≤ 剩余元气 && 每色需求 ≤ 对应气存量 + 无极存量`。
- **支付顺序**：先扣本色气，缺口自动扣无极之气（默认自动，可在设置中加确认开关——见第 10 节）。
- **阴气增费**：检查与支付时，对应颜色持有 N 层阴气则该色费用 +N（同色至多 +2）；死气对应无色费用（元气）。
- **减费**：不进入通用结算模板；个别绝世卡/特殊节点如需减费效果，按特殊效果单独实现（引擎 `modifiedCost` 机制可复用）。
- 玩家入队（battle.dart `_enqueueCard`）与打出扣费（`_playCard`）、敌方 AI 过滤三处同步扩展。
- **删除** card_script.ht 约 10 个 `_exhaust` 脚本变体，消耗逻辑由费用检查取代；引用这些脚本的卡牌改回基础脚本变体。

## 4. 气的产出与回合行为（新产出模型）

**统一生命周期**：所有资源气（元气、四色、无极，及 6 种阴气）在持有者**下个回合开始时清空**（决策 5）；自己回合结束时按剩余量做各类结算，图标与被动效果保留至清空。

> 注：本节中所有"节点加成/战斗开始种子"来自流派天赋节点（§7），**本轮天赋树不动，加成与种子按 0 处理**——机制代码要把"读取节点加成"写成可空/缺省 0 的形式，后续补上节点数据即自动生效。

### 4.1 元气（无色费用池）

- **实施决策：energy 计数器转换为 energy_positive_life 状态层数**——元气就是状态，不再是独立计数器。回合开始清空残留后获得 rank+3 层（获得时与死气自然对冲）；打牌支付无色费用 = 移除元气层数；EnergyDisplay 与支付检查改读状态层数。
- **回血**：自己回合结束时每剩余 1 层回复 2% 生命上限，**每层至少回复 1 点生命**（取代旧的"消耗 1 层回 5%"回调）。
- 回血后图标保留（对方可见剩余量），下个回合开始清空。
- gain_resource_vigor 等产元气卡改为直接增加元气层数（= 本回合可用费用，用不完则转化为回合结束回血）。

### 4.2 剑气（御剑/武器系）

- **第一回合**：获得 = 战斗开始种子（后续由御剑流派节点提供，本轮为 0）。
- **第二回合起**：回合开始时获得 = **上一回合打出的武器攻击牌数量** + 节点加成（本轮为 0）。
- 下个回合开始时清空。
- 实现：需追踪"本回合打出武器攻击牌数"（turnFlags 或 BattleCharacter 新增统计字段，回合开始时结转）。enable_chakra 的原即时触发（武器牌造成伤害 +1 剑气）由该模型取代。

### 4.3 怒气（锻体/徒手系）

- **第一回合**：获得 = 战斗开始种子（后续由锻体流派节点提供，本轮为 0）。
- **第二回合起**：回合开始时获得 = **上一回合自身受到的伤害 ÷ 10**（向下取整）+ 节点加成（本轮为 0）。
- 下个回合开始时清空（Phase 0 已删一处减半，剩余一处随本模型删除）。
- **保留风险代价**：每层怒气使自己受伤 +5%——在统一生命周期（决策 5）下该代价在对方回合实际生效；**未花掉的怒气在对方回合是防御风险**，花掉怒气同时是卸除该 debuff。
- enable_rage 的原即时触发由该模型取代。

### 4.4 灵气（悟道/法术系）

- 战斗开始时按灵力/10 获得（上限 manaMax），卡牌/装备可额外产出。
- **回合开始额外获得 = 悟道节点加成**（本轮为 0，机制预留）。
- 下个回合开始时清空（原"保留"改为统一生命周期；溢出天赋改造见 4.6）。

### 4.5 煞气（炼魂/咒术系，特殊）

- 跨战斗永久积累池 karma 保留（战后获得、战前带入上限 karmaMax = 念力/10）。
- 天赋开通后：每回合开始从 karma 池提取一部分作为当回合煞气（**提取量 = min(karmaMax, max(1, 池存量 ~/ 10))**，karmaMax = 念力/10 作为提取上限，节点加成后续叠加；**战斗开始时不再把 karma 一次性转为煞气**，移除 battle.dart 中的战前转换逻辑）。
- 回合结束记录未用量；**下个回合开始清空时，未使用的煞气返回 karma 池**。

### 4.6 溢出效果统一改为"回合结束未使用"触发，且均为增益型

原则：**剩余不用不再有惩罚，只作为特殊天赋/装备可以利用的资源**：

- 灵气：`overflowed_mana_*` 天赋（随机元素伤害 / 转元气）按剩余灵气层数触发（现有设计延续）。
- 剑气：**删除原"溢出→随机 debuff"惩罚**；剩余剑气默认直接清空，只有特定天赋/装备能将剩余剑气转化为收益（具体效果随节点赋能后续设计）。
- 结算时机：自己回合结束按剩余量触发；图标保留至下个回合开始清空。
- 上限概念（manaMax/chakraMax）在新模型下重新评估：上限只约束单次获得，不再约束持有。

### 4.7 节奏特点提示

剑气/怒气的产出是**滞后一轮的滚雪球模型**：上回合没打武器牌 → 本回合断剑气；不被打 → 断怒气。这是有意的节奏设计（武器系要持续进攻、徒手系要以伤换力）。本轮节点种子/加成为 0，第一回合剑气/怒气为 0 属预期（构筑深度后续由节点数据补足）。

### 4.8 软狂暴：劫气（与死气拆分）

- 新状态 `tribulation`（劫气）已在 Phase 1 建立（status_effect.json5 + status_script.ht `tribulation_self_turn_start`）：回合开始消耗 1 层，失去 10% 生命上限；非资源、不对冲、不影响费用、不占资源槽位。
- Phase 3 将 battle.dart 中每 16 回合的死气自动叠层改为叠劫气；死气仅通过卡牌/效果互动施加，抵消元气、附加无色增费（决策 9）。

## 5. 阴阳净值显示、对冲与图标映射

- 每种资源气在 UI 上只占**一个槽位**，显示净值 = 阳气层数 − 阴气层数。
- 净值为负时显示同一图标的**反色/深色背景版本**（运行时用 ColorFilter 程序化反色，不出新图）。
- 对冲时机：获得阳气时先抵消持有的对应阴气（含回合开始产出时的获得）；`kOppositeStatus` 已收窄为 6 对资源气（Phase 1 完成）。劫气不是资源，无对冲。
- 悬浮说明需同时解释净值的构成（"剑气 2 − 止戈之气 5 = −3"）。

**图标映射**（Phase 1 已修复 status_effect.json5 的 20 处死链）：

| 状态 ID | 新 icon 路径 |
|---|---|
| energy_positive_life（元气） | icon/cost/qi_basic.png |
| energy_positive_spell（灵气） | icon/cost/qi_mana.png |
| energy_positive_weapon（剑气） | icon/cost/qi_chakra.png |
| energy_positive_unarmed（怒气） | icon/cost/qi_rage.png |
| energy_positive_curse（煞气） | icon/cost/qi_karma.png |
| energy_positive_ultimate（无极之气） | icon/cost/qi_ultimate.png |
| energy_negative_life（死气） | icon/status/decay.png |
| energy_negative_spell/weapon/unarmed/curse/ultimate | 各自对应阳气的 icon/cost 路径（运行时反色） |
| penetrate/crit(→幸运)/ward/shield(→护盾) 及其对应 4 种阴气（均转普通状态） | 暂用 icon/status/status_placeholder.png，图标人工后续制作 |

## 6. UI 设计需求

### 6.1 卡面（暂缓，人工完成）

- 目标方案（万智牌式）：右上角费用区改为 pip 组合 `[无色数字徽章] [有色pip×n]`，有色 pip 用 icon/cost 图标。涉及较复杂美术设计，**本轮不做，之后人工完成**。
- **过渡期方案（本轮实现）**：卡面维持现有单数字无色费用徽章；有色费用以**自动生成的文本行**附在卡牌描述区（如"另需：剑气×2 / 怒气×1+煞气×1"），保证信息可用。数据接口（costPips）在代码侧预留。
- 减费绿字（引擎 `modifiedCost`）：不再是常规功能，仅当特殊减费效果存在时使用。

### 6.2 战斗界面

- **能量瓶**（已有）暂时保留显示元气池（原图标已弃用，可换 qi_basic.png，随 Phase 4 处理）。
- **气池不常显**：维持现状"出现才显示"——一副构筑通常只有 1-2 个主用颜色，常显 4 色 + 无极反而制造噪音。显示时遵循第 5 节净值规则。资源图标在对方回合期间保留显示（决策 5），对方可看到你的剩余费用。
- **可打出高亮**：不满足费用的手牌置灰（现逻辑只查能量，扩展为有色费用检查）；置灰卡悬浮提示缺什么（"剑气不足：需要 3，拥有 1"）。这是支付决策的主要信息渠道，弥补气池不常显。
- **支付反馈**：打出时有色费用从角色对应气图标处扣除（第一版用数字跳动 + 图标闪烁即可，动画后续迭代）。

### 6.3 牌组编辑界面（card_library）——暂缓

费用曲线面板、颜色分布面板随卡牌构筑界面的整体工作后续单独做，本轮不动。

### 6.4 角色属性面板（lib/widgets/character/stats.dart）

- 节点产出加成条目随天赋树工作后续做；本轮仅视情况补 karma 池当前值/每回合提取量信息（低优先级，可缓）。

### 6.5 美术资源清单

| 资源 | 状态 |
| ---- | ---- |
| 6 种费用图标（qi_basic/mana/chakra/rage/karma/ultimate） | ✅ 已完成（用户提供，icon/cost/） |
| 死气图标 decay.png | ✅ 已完成（用户提供） |
| 勾玉基础图（蓝）qi_magatama_base_v2.jpg | ✅ 已生成，其他颜色由用户编辑派生 |
| 6 种阴气反色图标 | 运行时 ColorFilter 程序化反色，无需出图 |
| 中立状态图标（穿透/幸运/清气/护盾 + 4 种阴） | 暂用 placeholder，人工后续制作 |
| 卡面 pip 组合设计（徽章底座、排列样式） | **暂缓，人工完成** |
| 劫气图标 | 暂缓，随卡面 pip 一起人工完成 |

## 7. 流派天赋节点赋能（产出型模板）——后续任务，本轮不实施

> 天赋树（passive_skills.json5 / passives.json5）本轮不动。以下模板供后续单独任务使用；本轮机制代码以"节点加成缺省 0"的形式预留接口。

15 个流派 rank 节点（track_5/6/7 系列）后续获得实质词条。**节点不提供减费，改为提供每回合资源产出**：

- **Rank 1 / 3 / 5**：每回合开始额外获得本流派颜色气 **+1**（累积，即 rank 5 时每回合 +3；平衡数值）。具体结算位置：
  - 悟道 → 灵气每回合 +X（4.4）；
  - 御剑 → 剑气每回合产出 +X，且提供**战斗开始种子**（如 rank 1 种子 2 点，平衡数值）（4.2）；
  - 锻体 → 怒气每回合产出 +X，且提供**战斗开始种子**（4.3）；
  - 炼魂 → 每回合 karma 提取量 +X（4.5）；
  - 法身 → 怒气与煞气各分一半（奇数时优先怒气，平衡待定）。
- **Rank 2 / 4**：流派标志性机制（悟道：灵气溢出相关；其余见 `plan/` 各流派文档）。
- 产出加成词条后续新增为天赋专属标记被动（passives.json5，沿用 enable_mana 系列模式），数值由节点数量推导，不在被动上带 value。
- 届时补齐 `swordcraft_rank_1` 等标记词条在 passives.json5 中的定义（当前缺失，有断言风险）。
- NPC 按 cultivationFavor 获得同样的境界节点被动（battle_entity.ht），保证镜像对局公平。

## 8. 实施步骤

- **Phase 0：清理** ✅ 已完成（删注释流派检查、`kResourceHasNegatives` 死代码、怒气双减半删一处、补 enable_mana 状态与脚本）。
- **Phase 1：数据层** ✅ 已完成（icon 死链修复、8 中立气转普通状态、劫气条目与脚本、kOppositeStatus 收窄、常量三处同步、costColored 加载校验、本地化改名）。注：曾加入的 `role` 字段已按讨论结论移除（冗余）。

### Phase 2：结算核心（进行中）
- card.ht / createBattleCard 写入 `cost`/`costColored`（含缺省推导规则）。
- 可打出检查与支付（含无极自动抵扣、阴气增费、同色增费上限 +2）。
- 删除 `_exhaust` 脚本变体；引用它们的卡牌改回基础脚本变体，并按原 exhaust 消耗量写入显式 `costColored`（上限 rank+1）。
- 过渡期卡面文本：描述区自动生成"另需：X气×N"行。
- 验收：测试脚本构造各流派卡，验证检查/支付/无极抵扣/阴气增费上限。

### Phase 3：气的产出与回合行为
- **统一生命周期改造**：所有资源气的清空时机从"自己回合结束"移到"下个回合开始时"（顺序：清空残留 → 结算新产出）；元气回血、溢出利用、煞气未用量记录仍在自己回合结束结算。
- 元气回血（2%/点、每点至少 1 点）；energy_positive_life 状态与 energy 计数器的归一（4.1 注）。
- 剑气/怒气新产出模型：滞后一轮结算（上回合武器牌数 / 上回合受伤 ÷10），节点加成/种子缺省 0；需新增回合统计字段；enable_chakra/enable_rage 回调重写；删除怒气剩余的一处减半。
- 灵气：回合结束清空；溢出天赋改为"回合结束未使用"触发（4.6）。
- 煞气 karma 池提取/返回（提取量 = max(1, karma/10)，节点加成缺省 0）。
- **劫气**：battle.dart 的死气自动叠层改为劫气；死气改为纯互动阴气。
- 验收：实战一局，验证资源跨对方回合保留、下回合开始清空、怒气受伤加成在对方回合生效、劫气叠层不影响费用。

### Phase 4：战斗 UI（仅代码部分；卡面 pip 渲染暂缓，人工完成）
- 气池净值 + 反色阴气显示（出现才显示，不常显；对方回合期间保留显示）。
- 手牌置灰 + 缺少资源提示；支付反馈（数字跳动 + 图标闪烁）。
- 能量瓶图标换用 qi_basic.png（或随卡面 pip 一起人工处理）。
- ~~引擎 `CustomGameCard` pip 渲染~~ → **暂缓，人工完成**。

### Phase 5：卡牌与词缀数据迁移
- 80 张卡逐卡复核 costColored（Phase 2 已按原 exhaust 消耗量处理 exhaust 卡；本阶段复核缺省推导结果是否合理）。
- card_affixes.json5：复核产气词缀数值（硬检查后产气价值上升）；评估启用被注释的剑气/怒气/煞气产气词缀（167-220 行）。
- `gain_resource_*` 8 张产气卡复核。

### Phase 6（暂缓，后续与天赋树工作一起）：流派节点赋能 + stats.dart 产出条目
### Phase 7：本地化收尾与文档（构筑面板暂缓）
- 本地化：旧称残留清理（battlecard.json 的"豪气/浩然之气"产气词条文案、craft.json、passive.json 的 start_battle 描述等）+ 新增条目定稿。
- 文档：`docs/docs/how2play/rpg/battle/` 三篇重写费用与气章节；`docs/docs/mod/battle/readme.md` 同步流程。
- 删除/归档 `plan-card_cost_pip.md`。
- ~~牌组编辑费用曲线/颜色分布面板~~ → 暂缓。

## 9. 主要涉及文件

| 模块 | 文件 |
|---|---|
| 费用检查 | lib/scene/battle/battle.dart、lib/scene/battle/character.dart、lib/logic/logic.dart |
| 卡牌数据 | assets/data/cards.json5、assets/data/card_affixes.json5、lib/data/game.dart、scripts/main/cardgame/card.ht、card_script.ht |
| 气行为 | assets/data/status_effect.json5、scripts/main/cardgame/status_script.ht、lib/scene/battle/common.dart |
| 引擎卡面（暂缓） | ../samsara-engine/lib/cardgame/custom_card.dart |
| 天赋（暂缓） | assets/data/passive_skills.json5、assets/data/passives.json5 |
| UI | lib/scene/battle/energy_display.dart、lib/widgets/battlecard/、lib/widgets/character/stats.dart、lib/scene/card_library/（暂缓） |
| 本地化 | assets/locale/zh/rpg/status_effect.json、battlecard.json、passive.json |
| 文档 | docs/docs/how2play/rpg/battle/*、docs/docs/mod/battle/readme.md |

## 10. 风险与平衡关注点

1. **滞后产出的断档**：本轮节点种子/加成为 0，剑气/怒气第一回合无产出属预期；后续节点数据补足。
2. **怒气的防御侧代价现在真实生效**（决策 5）：持有高怒气进入对方回合可能猝死，怒气产出数值（受伤÷10）需与 +5%/层的代价一起平衡。
3. **有色费用卡手**：低有色数量为主；高有色需求卡集中在流派构筑深处。
4. **敌方体验对等**：NPC 按流派集中加点/组卡，天然适配；Phase 5 后实测 NPC 是否会出现"一手打不出去的有色卡"。
5. **产气价值上升**：硬检查后产气卡/词缀变为构筑基石，Phase 5 统一复核数值。
6. **空过补偿与龟缩流**：元气回血（2%/点、保底 1 点）可能鼓励空过，必要时加每回合回血上限；劫气软狂暴保留，拖局仍有硬上限。
7. **阴气增费锁死**：同色至多 +2 上限防止某色费用被完全锁死。
8. **节点产出强度**（后续任务时评估）：每回合 +X 本色气是直接的费

用供给；rank 5 累积 +3 可能偏多，可降为 +1/+1/+2 曲线。

## 11. 遗留待决策（不影响主体开工）

1. **能量池命名**：战斗 UI 里元气池需要一个不与悟道"灵气"混淆的称呼（候选：真元/法力/灵力值）。
2. **劫气图标**：条目已定（tribulation），图标暂缓人工制作。
3. **符箓的新定位**：原卖点"消除流派需求"已失效，候选方向：突破境界限制使用 / 纯消耗品复制。可延后。
4. **无极抵扣确认开关**：默认自动抵扣，是否在设置中加"支付前确认"选项。
5. **法身的节点产出分配**（后续任务时定）：怒气/煞气各分一半，奇数时优先哪边。
