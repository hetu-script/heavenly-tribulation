# 战斗费用系统重构计划：统一气费用模型

> 状态：设计已确认，待实施。本文档只含计划，不改动现有代码。
> 本文档吸收并取代旧文档的费用模型与 UI 部分。与旧文档的**两处分歧**已按最新讨论修正：① 气池不常显；② 流派节点不提供减费，改为提供每回合额外资源（减费仅保留为绝世卡/特殊节点效果）。

## 1. 背景与现状（2026-09 调研结论）

当前战斗资源体系的实际情况（与口头描述有偏差，以代码为准）：

- **流派限制已不存在**：`lib/logic/logic.dart:607-625` 的 genre 检查已于 2026-05-15 注释，现仅检查境界/装备/属性。
- **资源不足无惩罚**：exhaust 词条（51/80 张卡使用）调用 `removeStatusEffect`，资源不足时"全有或全无"——一点也不扣，仅弹灰色提示，卡牌照常结算（`lib/scene/battle/character.dart:456-466`）。旧的"差额转阴气"机制已于 2026-05-05 注释。
- **20 种气中近半是"伪资源"**：正气、豪气、清气、浩然之气只有产气卡、无任何卡消耗；无极之气未出现在 cards.json5 中。
- 现存顺带问题（Phase 0 一并处理）：
  - `enable_mana` 状态在 status_effect.json5 中不存在，battle.dart:201-204 的添加调用无效；
  - 怒气回合结束存在两处减半（资源自身回调 + enable_rage 回调）；
  - `self_deck_end` 回调 Dart 侧从未派发（灵气洗牌回复未实现）；
  - `cultivation.dart:1689` 突破后 `rank = difficulty` 疑似应为 +1（单独确认，不在本计划范围内）。

## 2. 已确认的设计决策

1. **取消卡牌流派限制**（追认现状）：仅保留境界/装备/属性限制。流派身份由软限制维持：资源不对称、节点产出加成、机制加成。
2. **费用硬检查**：资源不足的牌不能打出。exhaust 词条消耗转化为卡牌有色费用，两个改动合并落地。
3. **万智牌式费用模型**：卡牌总费用 = rank + 1，由无色部分（元气支付）+ 有色部分（四色气支付）组成。
4. **元气 = 无色费用**：每回合开始获得 rank + 3（现状不变，不累积）；回合结束按剩余量回血（见 4.1）作为空过补偿；图标保留至下个回合开始时清空（见决策 5）。
5. **资源气的生命周期 = 一整轮**：四色气、无极之气（以及元气）在**持有者的下个回合开始时清空**，而非自己回合结束时——即持有期间覆盖对方的整个回合。理由：
   - 怒气的"每层受伤 +5%"等防御侧效果，受伤发生在对方回合，必须让资源活到对方回合才有意义；
   - 对方能看到你没花完的费用（炉石式信息）；
   - 没花掉的怒气在对方回合是防御风险——花掉怒气既是进攻收益也是卸除防御 debuff。
   回合结束的结算（元气回血、溢出利用、煞气记录未用量）仍在自己回合结束按剩余量进行，仅清空时机延后。
6. **无极之气 = 万能色**：落地"可支付任意有色费用"，优先扣本色气、不足自动用无极补差；保留稀有度（rank 4 产气词缀）。
7. **煞气特殊化**：跨战斗永久积累池（karma）保留；天赋开通后每回合开始从 karma 池提取一部分作为当回合煞气；回合结束记录未用量，**下个回合开始清空时未使用的煞气返回 karma 池**（而非消失）。
8. **中立气转普通状态**：正气、豪气（→"幸运"）、清气、浩然之气（→"护盾"）取消资源属性，行为不变。
9. **阴气精简为 6 种资源**：保留与元气、四色、无极对应的死气、逆灵之气、止戈之气、非战之气、无心之气、虚空之气；除现有负面效果外，统一附加"对应费用 +1/层"的增费作用（同色至多 +2）。戾气、衰气、浊气、萧索之气取消资源属性，转为普通 debuff 状态（行为不变）。
10. **阴气不入基础卡费用**：极少数绝世卡可例外（exotic 费用，如"需求 3 层死气"），保留绝世词条利用阴气的扩展空间。
11. **阴阳净值显示**：每种资源气只占一个 UI 槽位，显示净值（阳 − 阴），阴大于阳时同一图标用反色/深底区分（见第 5 节）。
12. **流派节点产出资源而非减费**：流派境界节点提供每回合额外的本流派颜色气（见第 7 节）。减费不再作为通用节点模板，仅保留为个别绝世卡/特殊节点的特殊效果（适用面窄可接受）。
13. **软狂暴机制与死气拆分**：新增非资源状态（暂定名"劫气"，贴合"战斗拖太久天道降劫"的主题，命名待定），行为与现行软狂暴完全相同（每 16 回合双方叠层、回合开始消耗 1 层失去 10% 生命上限），但**不参与阴阳对冲、不影响费用**。死气回归纯粹的"元气对应阴气"定位（仅通过卡牌/效果互动施加，抵消对方元气、增加无色费用），不再自动叠层。

## 3. 费用数据模型（继承旧文档，含调整）

### 3.1 卡牌实例与 cards.json5

```
card.cost      // 无色费用（int），引擎渲染与现有代码继续用
card.costChi   // 有色费用映射，如 { spell: 3 } 或 { unarmed: 2, curse: 1 }（法身双色）
               // 无流派卡为 null 或 {}
// 不变量：cost + ΣcostChi.values = rank + 1（校验脚本保证）
```

- cards.json5 主词条新增可选字段 `costChi`。
- **缺省推导**：有流派的卡按"有色 = ⌈(rank+1)/2⌉、无色 = ⌊(rank+1)/2⌋"分配，颜色由 genre→颜色映射决定；无流派卡全无色。逐卡微调时可参考该卡原 exhaust 消耗量填写显式 `costChi`。
- **显式填写优先**：用于特殊卡；校验总费用不变量，且基础卡的费用色必须属于 4 色（exotic 费用仅允许 `isUnique: true` 的卡）。

### 3.2 genre → 费用色映射（常量，三处同步）

```
spellcraft → spell（灵气）
swordcraft → weapon（剑气）
bodyforge  → unarmed（怒气）
vitality   → curse（煞气）
avatar     → unarmed + curse（双色，对半拆；奇数时无色少 1）
```

同步点：`lib/data/common.dart`、`lib/data/constants.dart`、`scripts/main/binding/constants.ht`。

### 3.3 气的角色声明

status_effect.json5 每种 `energy_*` 条目加 `role` 字段：

```
energy_positive_spell / weapon / unarmed / curse: role: "cost"
energy_positive_ultimate:                          role: "wildcard"
其余（含 6 种保留的阴气）:                          role: "status"（可省略为默认）
```

费用结算代码只认 `role == "cost"`（及 wildcard 抵扣），从代码层面杜绝阴气/状态气入费用。注意：6 种保留的阴气虽然仍是资源（`isResource`），但角色为 status——它们通过"增费"间接影响费用，而非直接作为费用。

### 3.4 支付与检查规则

- **可打出检查**：`无色费用 ≤ 剩余元气 && 每色需求 ≤ 对应气存量 + 无极存量`。
- **支付顺序**：先扣本色气，缺口自动扣无极之气（默认自动，可在设置中加确认开关——见第 11 节）。
- **阴气增费**：检查与支付时，对应颜色持有 N 层阴气则该色费用 +N（同色至多 +2）。
- **减费**：不进入通用结算模板；个别绝世卡/特殊节点如需减费效果，按特殊效果单独实现（引擎 `modifiedCost` 机制可复用）。
- 玩家入队（battle.dart:822-827）与打出扣费（:900-902）、敌方 AI 过滤（:1042-1065）三处同步扩展。
- **删除** card_script.ht 约 10 个 `_exhaust` 脚本变体，消耗逻辑由费用检查取代。

## 4. 气的产出与回合行为（新产出模型）

**统一生命周期**：所有资源气（元气、四色、无极，及 6 种阴气）在持有者**下个回合开始时清空**（决策 5）；自己回合结束时按剩余量做各类结算，图标与被动效果保留至清空。

### 4.1 元气（无色费用池）

- 每回合开始获得 rank + 3（先清空上回合残留，再重新获得）。
- **回血**：自己回合结束时每剩余 1 点元气回复 2% 生命上限，**每点至少回复 1 点生命**（防止低生命上限时取整为 0）。
- 回血后图标保留（对方可见剩余量），下个回合开始清空。

### 4.2 剑气（御剑/武器系）

- **第一回合**：获得 = 战斗开始种子（由御剑流派节点提供，见第 7 节）。
- **第二回合起**：回合开始时获得 = **上一回合打出的武器攻击牌数量** + 节点加成。
- 下个回合开始时清空。
- 实现：需追踪"本回合打出武器攻击牌数"（turnFlags 或 BattleCharacter 新增统计字段，回合开始时结转）。enable_chakra 的原即时触发（武器牌造成伤害 +1 剑气）由该模型取代。

### 4.3 怒气（锻体/徒手系）

- **第一回合**：获得 = 战斗开始种子（由锻体流派节点提供）。
- **第二回合起**：回合开始时获得 = **上一回合自身受到的伤害 ÷ 10**（向下取整）+ 节点加成。
- 下个回合开始时清空（取代现有两处减半逻辑，一并删除）。
- **保留风险代价**：每层怒气使自己受伤 +5%——在统一生命周期（决策 5）下该代价在对方回合实际生效；**未花掉的怒气在对方回合是防御风险**，花掉怒气同时是卸除该 debuff。
- enable_rage 的原即时触发由该模型取代。

### 4.4 灵气（悟道/法术系）

- 战斗开始时按灵力/10 获得（上限 manaMax），卡牌/装备可额外产出。
- **回合开始额外获得 = 悟道节点加成**（原"每回合无产出"由节点产出补齐，见第 7 节）。
- 下个回合开始时清空（原"保留"改为统一生命周期；溢出天赋改造见 4.6）。

### 4.5 煞气（炼魂/咒术系，特殊）

- 跨战斗永久积累池 karma 保留（战后获得、战前带入上限 karmaMax = 念力/10）。
- 天赋开通后：每回合开始从 karma 池提取一部分作为当回合煞气（默认提取量 = max(1, karma/10)；**炼魂节点加成提高提取量**，见第 7 节）。
- 回合结束记录未用量；**下个回合开始清空时，未使用的煞气返回 karma 池**。

### 4.6 溢出效果统一改为"回合结束未使用"触发，且均为增益型

原各资源"超过上限时的溢出效果"统一改造，原则：**剩余不用不再有惩罚，只作为特殊天赋/装备可以利用的资源**：

- 灵气：`overflowed_mana_*` 天赋（随机元素伤害 / 转元气）按剩余灵气层数触发（现有设计延续）。
- 剑气：**删除原"溢出→随机 debuff"惩罚**；改为与灵气同模式——剩余剑气默认直接清空，只有特定天赋/装备能将剩余剑气转化为收益（如"回合结束每剩余 1 点剑气，下回合第一张武器攻击牌 +X 伤害"之类，具体效果在 Phase 6 落地时设计）。
- 结算时机：自己回合结束按剩余量触发；图标保留至下个回合开始清空。
- 上限概念（manaMax/chakraMax）在新模型下重新评估：上限只约束单次获得，不再约束持有。

### 4.7 节奏特点提示

剑气/怒气的产出是**滞后一轮的滚雪球模型**：上回合没打武器牌 → 本回合断剑气；不被打 → 断怒气。这是有意的节奏设计（武器系要持续进攻、徒手系要以伤换力），节点加成（4.2/4.3 的固定 +X）同时充当每回合的保底产出，降低断档风险。

### 4.8 软狂暴：劫气（新状态，与死气拆分）

- 新增非资源状态（暂定名"劫气"，命名待定）：行为与现行软狂暴完全一致——每 16 回合双方各叠 `回合数/16` 层（battle.dart:972-975 的死气叠层代码改为叠劫气）；回合开始消耗 1 层，失去 10% 生命上限。
- **不是资源**：无 isResource、不参与阴阳对冲、不影响费用、不显示在资源槽位（显示在普通状态区）。
- 死气保留为元气对应的资源阴气：仅通过卡牌/效果互动施加，抵消元气、附加无色增费（决策 9），不再自动叠层。

## 5. 阴阳净值显示与对冲

- 每种资源气在 UI 上只占**一个槽位**，显示净值 = 阳气层数 − 阴气层数。
- 净值为负时显示同一图标的**反色/深色背景版本**，表示阴气（美术：6 张反色变体，可用 shader/ColorFilter 程序化生成，避免新图）。
- 对冲时机：获得阳气时先抵消持有的对应阴气（含回合开始产出时的获得），沿用 `kOppositeStatus`（lib/scene/battle/common.dart:28-47）机制，收窄为 6 对资源气；转为普通状态的 4 对移除对冲。劫气不是资源，无对冲。
- 悬浮说明需同时解释净值的构成（"剑气 2 − 止戈之气 5 = −3"）。

## 6. UI 设计需求（继承旧文档问题二，含修正）

### 6.1 卡面

- 右上角费用区改为 pip 组合：`[无色数字徽章] [有色pip×n]`。
  - 无色部分：数字徽章（沿用现有样式）。
  - 有色部分：每点一个小圆图标横排紧跟右侧，最多 3-4 个，约费用数字一半高。
  - 绝世卡 exotic 费用（阴气/状态气需求）用对应状态图标做 pip，天然兼容。
  - 减费绿字（引擎 `modifiedCost`）：不再是常规功能，仅当特殊减费效果存在时使用。
- 实现：**方案 A（推荐）**——扩展引擎 `CustomGameCard`（`../samsara-engine/lib/cardgame/custom_card.dart`）加 `costPips: List<String>`；方案 B（本项目挂 Flame 子组件覆盖渲染）作为备选。

### 6.2 战斗界面

- **能量瓶**（已有）继续表示元气池。
- **气池不常显**：维持现状"出现才显示"——一副构筑通常只有 1-2 个主用颜色，常显 4 色 + 无极反而制造噪音。显示时遵循第 5 节净值规则。资源图标在对方回合期间保留显示（决策 5），对方可看到你的剩余费用。
- **可打出高亮**：不满足费用的手牌置灰（现逻辑只查能量，扩展为 pip 检查）；置灰卡悬浮提示缺什么（"剑气不足：需要 3，拥有 1"）。这是支付决策的主要信息渠道，弥补气池不常显。
- **支付反馈**：打出时有色 pip 从角色气图标处飞扣（第一版用数字跳动 + 图标闪烁即可，动画后续迭代）。

### 6.3 牌组编辑界面（card_library）

- **费用曲线面板**：按总费用（rank+1）统计卡组各费段数量。
- **颜色分布面板**：统计卡组有色 pip 的四色分布，直观显示流派依赖度。

### 6.4 角色属性面板（lib/widgets/character/stats.dart）

- 增加"每回合资源产出"条目，显示当前生效的节点产出加成（如"御剑 Rank 1/3/5：每回合剑气 +3（战斗开始种子 +2）"），让玩家能查清资源来源。
- 增加 karma 池当前值/每回合提取量信息。

### 6.5 美术资源清单

| 资源 | 状态 | 获取方式 |
| ---- | ---- | -------- |
| 无色费用数字徽章底座 | 新增 | gemini-image-gen skill 出初稿，需人工精修 |
| 4 色费用 pip 小图标 | 现有状态图标可复用 | 优先生成小尺寸简化版；效果不好则暂时缩放复用 |
| 无极之气 pip | 复用现有图标 | 无需新作 |
| 6 种阴气反色图标 | 新增 | 优先程序化反色（ColorFilter），效果不好再用 skill 出图 |
| 劫气图标 | 新增 | skill 出初稿（劫雷/天谴意象），需人工收敛 |
| 阴气 exotic pip（绝世卡） | 复用现有 | 无需新作 |

图标命名一律与状态 ID 对齐（旧文档教训：leech/pure 等旧命名已造成引用失效）。

## 7. 流派天赋节点赋能（产出型模板）

15 个流派 rank 节点（passive_skills.json5 track_5/6/7 系列）获得实质词条。**节点不提供减费，改为提供每回合资源产出**：

- **Rank 1 / 3 / 5**：每回合开始额外获得本流派颜色气 **+1**（累积，即 rank 5 时每回合 +3；平衡数值）。具体结算位置：
  - 悟道 → 灵气每回合 +X（4.4）；
  - 御剑 → 剑气每回合产出 +X，且提供**战斗开始种子**（如 rank 1 种子 2 点，平衡数值）（4.2）；
  - 锻体 → 怒气每回合产出 +X，且提供**战斗开始种子**（4.3）；
  - 炼魂 → 每回合 karma 提取量 +X（4.5）；
  - 法身 → 怒气与煞气各分一半（奇数时优先怒气，平衡待定）。
- **Rank 2 / 4**：流派标志性机制（悟道：灵气溢出相关；其余见 `plan/` 各流派文档）。
- 产出加成词条新增为天赋专属标记被动（passives.json5，沿用 enable_mana 系列模式），数值由节点数量推导，不在被动上带 value（避免与节点重复获得的被动合并逻辑冲突）。
- 补齐 `swordcraft_rank_1` 等标记词条在 passives.json5 中的定义（当前缺失，有断言风险）。
- NPC 按 cultivationFavor 获得同样的境界节点被动（battle_entity.ht），保证镜像对局公平。
- 减费效果如需存在，仅出现在个别绝世卡/特殊节点上，单独设计。

## 8. 实施步骤

### Phase 0：清理（独立可交付）
- 删除 logic.dart:607-625 已注释的流派检查；修正 character.dart:438-441 过时注释、删死代码 `kResourceHasNegatives`。
- 修复或移除 battle.dart:201-204 的 `enable_mana` 无效调用。
- 删除怒气两处减半（由 4.3 新模型取代，若 Phase 顺序调整则先保留一处）。

### Phase 1：数据层
- status_effect.json5：加 `role` 字段；中立 4 阳 4 阴转普通状态（去 isResource、改名浩然→护盾/豪气→幸运、更新描述）；**新增劫气状态条目**（非资源）。
- 常量三处同步：genre→费用色映射、费用色列表。
- cards.json5 支持 `costChi` + 校验脚本（总费用不变量、基础卡仅限 4 色）。
- `kOppositeStatus` 收窄为 6 对。

### Phase 2：结算核心
- card.ht / createBattleCard 写入 `cost`/`costChi`。
- 可打出检查与支付（含无极自动抵扣、阴气增费、同色增费上限 +2）。
- 删除 `_exhaust` 脚本变体，相关卡改回基础脚本。
- 验收：测试脚本构造各流派卡，验证检查/支付/无极抵扣/阴气增费上限。

### Phase 3：气的产出与回合行为
- **统一生命周期改造**：所有资源气的清空时机从"自己回合结束"移到"下个回合开始时"（顺序：清空残留 → 结算新产出）；元气回血、溢出利用、煞气未用量记录仍在自己回合结束结算。
- 元气回血（2%/点、每点至少 1 点）。
- 剑气/怒气新产出模型：战斗开始种子 + 滞后一轮结算（上回合武器牌数 / 上回合受伤 ÷10）+ 节点加成，需新增回合统计字段；enable_chakra/enable_rage 回调重写。
- 灵气：回合开始节点加成；溢出天赋改为"回合结束未使用"触发（4.6）。
- 煞气 karma 池提取/返回；节点加成提高提取量；调整 enable_karma 描述。
- **劫气**：battle.dart:972-975 的死气自动叠层改为劫气；死气改为纯互动阴气。
- 验收：实战一局，验证资源跨对方回合保留、下回合开始清空、怒气受伤加成在对方回合生效、劫气叠层不影响费用。

### Phase 4：卡面与战斗 UI
- 引擎 `CustomGameCard` 加 pip 渲染（方案 A）；createBattleCard 传入 pip 数据。
- 气池净值 + 反色阴气显示（出现才显示，不常显；对方回合期间保留显示）。
- 手牌置灰 + 缺少资源提示；支付反馈（数字跳动 + 图标闪烁）。
- 敌方 AI 同步新检查。
- 依赖：至少占位美术资源到位（可先缩放复用现有图标）。

### Phase 5：卡牌与词缀数据迁移
- 80 张卡按缺省推导规则批量生成 costChi，逐卡复核（参考原 exhaust 消耗量微调）。
- card_affixes.json5：复核产气词缀数值（硬检查后产气价值上升）；评估启用被注释的剑气/怒气/煞气产气词缀（167-220 行）。
- `gain_resource_*` 8 张产气卡复核。

### Phase 6：流派天赋节点赋能
- 按第 7 节落地 15 个节点的产出加成/机制/种子词条；补齐 `*_rank_*` 标记词条定义。
- 设计剑气剩余利用的天赋/装备效果（4.6）。
- stats.dart"每回合资源产出"条目与 karma 池信息。

### Phase 7：构筑面板、本地化与文档
- 牌组编辑：费用曲线 + 颜色分布面板。
- 本地化：重点 `assets/locale/zh/rpg/status_effect.json` 与 `rpg/battlecard.json`（气相关条目约 110 处中实际需改写的子集）；新增劫气的名称与描述。
- 文档：`docs/docs/how2play/rpg/battle/` 三篇重写费用与气章节（resource/readme.md 写新产出模型、统一生命周期与阴阳净值、card/readme.md 写费用模型）；`docs/docs/mod/battle/readme.md` 同步流程。
- 删除/归档 `plan-card_cost_pip.md`（内容已被本文档吸收）。

## 9. 主要涉及文件

| 模块 | 文件 |
|---|---|
| 费用检查 | lib/scene/battle/battle.dart、lib/scene/battle/character.dart、lib/logic/logic.dart |
| 卡牌数据 | assets/data/cards.json5、assets/data/card_affixes.json5、lib/data/game.dart、scripts/main/cardgame/card.ht、card_script.ht |
| 气行为 | assets/data/status_effect.json5、scripts/main/cardgame/status_script.ht、lib/scene/battle/common.dart |
| 引擎卡面 | ../samsara-engine/lib/cardgame/custom_card.dart |
| 天赋 | assets/data/passive_skills.json5、assets/data/passives.json5 |
| UI | lib/scene/battle/energy_display.dart、lib/widgets/battlecard/、lib/widgets/character/stats.dart、lib/scene/card_library/ |
| 本地化 | assets/locale/zh/rpg/status_effect.json、battlecard.json、passive.json |
| 文档 | docs/docs/how2play/rpg/battle/*、docs/docs/mod/battle/readme.md |

## 10. 风险与平衡关注点

1. **滞后产出的断档**：剑气/怒气断粮回合的体验（见 4.7），节点加成与种子是兜底，实测后调数值。
2. **节点产出强度**：每回合 +X 本色气是直接的费用供给，比旧"减费"更直白也更强；rank 5 累积 +3 可能偏多，实测后可降为 +1/+1/+2 曲线。
3. **怒气的防御侧代价现在真实生效**（决策 5）：持有高怒气进入对方回合可能猝死，怒气产出数值（受伤÷10）需与 +5%/层的代价一起平衡。
4. **有色费用卡手**：低有色数量为主；高有色需求卡集中在流派构筑深处。
5. **敌方体验对等**：NPC 按流派集中加点/组卡，天然适配；Phase 5 后实测 NPC 是否会出现"一手打不出去的有色卡"。
6. **产气价值上升**：硬检查后产气卡/词缀变为构筑基石，Phase 5 统一复核数值。
7. **空过补偿与龟缩流**：元气回血（2%/点、保底 1 点）可能鼓励空过，必要时加每回合回血上限；劫气软狂暴保留，拖局仍有硬上限。
8. **阴气增费锁死**：同色至多 +2 上限防止某色费用被完全锁死。

## 11. 遗留待决策（不影响主体开工）

1. **能量池命名**：战斗 UI 里元气池需要一个不与悟道"灵气"混淆的称呼（候选：真元/法力/灵力值）。
2. **劫气命名与图标**：暂定"劫气"，可在本地化阶段定稿。
3. **符箓的新定位**：原卖点"消除流派需求"已失效，候选方向：突破境界限制使用 / 纯消耗品复制。可延后。
4. **无极抵扣确认开关**：默认自动抵扣，是否在设置中加"支付前确认"选项。
5. **法身的节点产出分配**：怒气/煞气各分一半，奇数时优先哪边。
