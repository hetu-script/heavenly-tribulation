# 卡牌费用 Pip 系统实施计划

> 本文档整理 2026-02 关于战斗费用系统的设计讨论结论，并围绕两个待解决问题给出实施计划：
> **问题一：卡牌费用数据如何记录。问题二：UI 如何显示（卡面 + 战斗/牌组界面）。**
> 美术部分使用 gemini-image-gen skill 出初稿，部分资源需要人工精修（标注为「需人工」）。

---

## 〇、截至目前的已定决策

### 流派与卡牌使用

1. **取消卡牌使用的流派限制，仅保留境界限制**（装备需求保留）。流派边界改由软限制维持：资源不对称、减费、机制加成。
2. **资源不足则不能打出卡牌**。废弃"无资源也可打出但获得阴气"的负债规则；负债机制未来可作为个别装备/天赋/绝世卡的构筑内容复活。
3. 符箓需要重新定位（原卖点"消除流派需求"失效），待定。

### 费用模型（MTG 风格 pip）

4. 卡牌总费用 = **境界 + 1**（最高 6），由两部分组成：
   - **无色费用**：用每回合能量支付（能量 = 境界 + 3，每回合回满）。
   - **有色费用**：用对应流派的阳气支付。费用色集合封闭，**永远只有 4 色**：灵气（悟道）、剑气（御剑）、怒气（锻体）、煞气（炼魂）。法身 = 双色（怒气+煞气）。
5. **有色与无色约对半分**（如满级卡 = 3 无色 + 3 有色），不允许"1 无色 + 5 气"——理由：气点价值远高于无色点（持有时有 +5 被动加成），且减费只作用于无色部分，无色太少会让境界节点的减费失效。
6. **无极之气 = 万能支付手段**（可抵扣任意有色费用），本身不是费用色。
7. **阴气永远不出现在基础卡牌的费用中**。极少数绝世卡牌可以例外（需求阴气/中立气的 exotic 费用，如"需求 3 层死气"）。
8. 废弃旧的"每个词条各自带费用、总费用为词条之和"模型（`docs/docs/how2play/rpg/battle/card/readme.md:43` 描述的模型，实际代码中也未实现）。**打造（精炼）改变卡牌威力但不改变费用；境界决定费用。**

### 气的角色分层（20 种气全部保留，不做类型删减）

| 角色 | 气 | 说明 |
| ---- | --- | ---- |
| `cost`（费用色） | 灵气、剑气、怒气、煞气 | 唯一允许进入费用槽的气 |
| `wildcard`（万能支付） | 无极之气 | 可支付任意有色费用，自身不是费用 |
| `status`（状态） | 其余全部（含所有阴气） | 卡牌/装备效果的词汇表，永不入费用 |

### 天赋盘境界节点模板（五流派统一）

9. Rank 1 / 3 / 5 = 本流派标签卡牌**无色费用 -1**（下限锁 1，不能到 0——"抽牌数 = 费用数 = 境界+3"体系下 0 费卡是白嫖引擎）。
10. Rank 2 / 4 = 流派标志性机制（悟道：灵气溢出相关；其余见 `plan/` 各流派文档）。
11. 减费后终局节奏：化神 8 能量，满级卡 6→3，每回合 = 2 张满级 + 1 张次级，或 1 满级 + 2 还婴 + 1 结丹（3-4 次行动/回合）。
12. NPC 按其 `cultivationFavor` 获得同样的境界节点被动，保证镜像对局公平（`scripts/main/data/character/battle_entity.ht`）。

---

## 一、现状代码事实（实施前必读）

- **卡牌费用已是"境界+1"**：`lib/data/game.dart:1436`（`createBattleCard`）中 `cost: rank + 1`，卡面右上角显示费用数字 + 境界图标（`cultivation/cultivation$rank.png`），左上角是流派图标（`battlecard/genre_$genre.png`）。
- **每回合能量 = 境界+3**：`lib/logic/logic.dart:941` `getHandLimitForRank`，`lib/scene/battle/battle.dart:981` 每回合回满；能量瓶 UI 已存在（`heroEnergyDisplay` / `GameUI.battleEnergyBottleSize`）。
- **打出检查在 `lib/scene/battle/battle.dart:818-826`（入队时）与 `:900-901`（打出扣费）**；敌人 AI 的可打出过滤在 `:1044-1054`。
- **引擎卡牌组件只支持单费用数字 + 单图标**：`../samsara-engine/lib/cardgame/custom_card.dart` 的 `cost` / `modifiedCost` / `costIconSpriteId`。`modifiedCost` 已存在，可直接用于减费后的变色显示。
- **四种费用色 + 无极的状态图标已存在**：`assets/images/icon/status/energy_positive_{spell,weapon,unarmed,curse,ultimate}.png`。
- **cards.json5 / card_affixes.json5 目前没有任何 cost 字段**——费用完全由 `rank+1` 推导，无历史数据包袱。
- **气的状态定义在 `assets/data/status_effect.json5`**，均带 `isResource: true`，尚无角色（cost/wildcard/status）区分。
- 常量三处同步点：`lib/data/common.dart`、`lib/data/constants.dart`、`scripts/main/binding/constants.ht`。

---

## 二、问题一：卡牌费用数据如何记录

### 2.1 数据结构

**卡牌实例（HTStruct）**：保持向后兼容的标量 + 映射双字段——

```
card.cost      // 无色费用（int），引擎渲染与现有代码继续用
card.costChi   // 有色费用映射，如 { spell: 3 } 或 { unarmed: 2, curse: 1 }（法身双色）
               // 无流派卡为 null 或 {}
// 总费用 = cost + ΣcostChi.values = rank + 1（不变量，校验脚本保证）
```

**cards.json5 主词条**：新增可选字段 `costChi`，如 `costChi: { spell: 3 }`。
- **缺省时按规则推导**：有流派（genre）的卡自动按"有色 = ⌈(rank+1)/2⌉、无色 = ⌊(rank+1)/2⌋"分配，颜色由 genre→颜色映射决定（见 2.2）；无流派卡全无色。
- **显式填写时优先**：用于特殊卡（某张悟道卡偏重无色、绝世卡的 exotic 费用如 `{ life_negative: 3 }`）。
- 校验：显式填写的 costChi + 无色部分必须等于 rank+1；基础卡的费用色必须属于 4 色（exotic 费用仅允许 `isUnique: true` 的卡）。

**genre → 费用色映射**（常量，三处同步）：

```
spellcraft → spell（灵气）
swordcraft → weapon（剑气）
bodyforge  → unarmed（怒气）
vitality   → curse（煞气）
avatar     → unarmed + curse（双色，对半拆；奇数时无色少 1）
```

### 2.2 气的角色声明

在 `assets/data/status_effect.json5` 每种 `energy_*` 条目上加 `role` 字段：

```
energy_positive_spell:   { ..., role: "cost" }      // 灵气
energy_positive_weapon:  { ..., role: "cost" }      // 剑气
energy_positive_unarmed: { ..., role: "cost" }      // 怒气
energy_positive_curse:   { ..., role: "cost" }      // 煞气
energy_positive_ultimate:{ ..., role: "wildcard" }  // 无极之气
其余全部:                { ..., role: "status" }     // 含所有阴气，可省略为默认
```

费用结算代码只认 `role == "cost"`（及 wildcard 抵扣），从代码层面杜绝阴气/中立气入费用。

### 2.3 减费数据

新增被动词条（`assets/data/passives.json5`，沿用 `enable_mana` 系列的"天赋专属标记"模式）：

```
cardcost_reduce_spellcraft: { id, description, priority: 39xx }  // 等 5 条
```

战斗中结算：打出检查前，按英雄已激活的 `*_rank_1/3/5` 节点被动，对本流派 genre 卡的无色费用 -1（下限 1）。数值直接由节点数量推导，不在被动上带 value——避免与"节点重复获得会累加"的现有被动合并逻辑（`passive_skills.json5` 头部注释）打架。

### 2.4 支付与检查规则

- **可打出检查**：`无色费用 ≤ 剩余能量 && 每色需求 ≤ 对应气存量 + 无极存量`（无极补任意色的缺口）。
- **支付顺序**：有色费用先扣对应色的气，缺口部分扣无极之气。**无极抵扣是否自动**见「待决策」。
- 入队检查（`battle.dart:818`）与打出扣费（`:900`）都要扩展为双色检查；敌人 AI 过滤（`:1044`）同步。

---

## 三、问题二：UI 如何显示

### 3.1 卡面显示

右上角费用区从"单图标 + 单数字"改为 **pip 组合**：

```
[无色数字徽章] [有色pip×n]
     3            🔵🔵🔵        ← 例：满级悟道卡
```

- 无色部分：**数字徽章**（沿用现有费用数字样式），底座换新图标（见美术清单）。
- 有色部分：每点一个小圆图标，横排紧跟无色徽章右侧。最多 3-4 个，小尺寸（约费用数字的一半高）。
- **减费显示**：引擎已有 `modifiedCost` 机制——减费后数字变绿；有色部分不受减费影响（减费只减无色）。
- 绝世卡 exotic 费用（如阴气需求）：同样用对应状态图标做 pip，天然兼容。
- 实现方式二选一：
  - **A（推荐）**：扩展引擎 `CustomGameCard`，加 `costPips: List<String>`（spriteId 列表），在现有 cost 图标区右侧逐个渲染。引擎是本地路径依赖，可直接改。
  - B：不改引擎，在本项目给卡牌挂一个 Flame 子组件覆盖渲染。侵入小但和引擎的 cost 区定位逻辑割裂。

### 3.2 战斗界面

- **能量瓶**（已有）：继续表示无色费用池，无需改动。
- **气池显示**：角色资源条已有状态图标 + 颜色（`lib/scene/battle/character.dart:54-63`）。需要保证 4 色费用气 + 无极之气在战斗 UI 上**始终可见、数量醒目**（即便为 0 也显示灰色图标），因为它们是支付决策的依据。其余状态维持现状（出现才显示）。
- **可打出高亮**：手牌中不满足费用的卡置灰（现有逻辑只查能量，`battle.dart:1026-1054`，扩展为 pip 检查）。置灰卡悬浮时提示缺什么（"剑气不足：需要 3，拥有 1"）。
- **支付反馈**：打出卡牌时，有色 pip 从角色对应气图标处飞扣（动画可后续迭代，第一版用数字跳动 + 图标闪烁即可）。

### 3.3 牌组编辑界面（card_library）

pip 模型下几乎免费获得的构筑工具，建议做：

- **费用曲线面板**：按总费用（rank+1）统计卡组各费段数量（万智 mana curve）。
- **颜色分布面板**：统计卡组有色 pip 的四色分布，直观显示"这副牌有多依赖剑气"。

### 3.4 角色属性面板（`lib/widgets/character/stats.dart`）

- 增加"流派减费"条目，显示当前生效的减费来源（如"御剑 Rank 1/3/5：本流派卡牌无色费用 -3"），让玩家能查清"这张牌为什么便宜了"。

---

## 四、美术资源清单

| 资源 | 状态 | 获取方式 |
| ---- | ---- | -------- |
| 无色费用数字徽章底座 | 新增 | gemini-image-gen skill 出初稿，**需人工**精修（尺寸/描边对齐现有卡框） |
| 4 色费用 pip 小图标（灵气/剑气/怒气/煞气） | 现有状态图标可复用，但尺寸偏大细节偏多 | 优先生成小尺寸简化版：skill 出初稿，**需人工**收敛细节；若效果不好则暂时直接缩放复用现有图标 |
| 无极之气 pip 图标 | 复用 `energy_positive_ultimate.png` | 无需新作 |
| 阴气 exotic pip（绝世卡用） | 复用现有 `energy_negative_*.png` | 无需新作 |
| pip 区底框/分隔（如需要） | 新增 | skill 初稿，**需人工** |

注意：`assets/images/icon/status/energy_positive_{leech,pure}.png` 等旧命名文件已不存在，相关引用已修复（status_effect.json5 中正气/豪气/戾气/衰气四处的 icon 路径已改为与状态 ID 同名），新做图标时**命名一律与状态 ID 对齐**。

---

## 五、分阶段实施

| 阶段 | 内容 | 验收点 |
| ---- | ---- | ------ |
| 0. 数据层 | status_effect.json5 加 `role`；常量三处同步（genre→色映射、费用色列表）；cards.json5 加 `costChi` 支持 + 校验脚本（总费用不变量、基础卡仅限 4 色） | 校验脚本跑过全部现有卡牌数据 |
| 1. 结算核心 | card.ht 生成卡牌时写入 `cost`/`costChi`；可打出检查与支付（含无极抵扣）；减费注入（读境界节点被动） | 测试脚本：构造各流派卡，验证检查/支付/无极抵扣/减费下限 |
| 2. 卡面 UI | 引擎 `CustomGameCard` 加 pip 渲染（方案 A）；`createBattleCard` 传入 pip 数据；减费绿字 | 卡牌库中各流派卡显示正确 pip |
| 3. 战斗 UI | 气池常显；手牌置灰 + 缺少资源提示；敌人 AI 同步新检查 | 实战一局，双方费用行为正确 |
| 4. 构筑面板 | 费用曲线 + 颜色分布；stats.dart 减费条目 | 牌组编辑界面可见两个面板 |
| 5. 收尾 | 绝世卡 exotic 费用支持；NPC 境界节点被动（battle_entity.ht）；同步文档 `docs/docs/how2play/rpg/battle/resource/readme.md`（清浊气、leech/pure 修复说明、气的角色表）和 `card/readme.md`（费用模型改写） | 文档与实现一致 |

阶段 0-1 是纯数据/逻辑，可先行；阶段 2 起依赖美术资源到位（至少无色徽章与 4 色 pip 的占位图，可先用现有图标缩放顶替）。

---

## 六、待决策问题（需要你拍板）

1. **无极抵扣的交互**：自动抵扣（打出时自动用无极补缺口，简单）还是支付前弹确认（无极珍贵，避免误用）？建议：默认自动，设置里给开关。
2. **有色 pip 的精确公式**：建议 `有色 = ⌈(rank+1)/2⌉`，即满级 3 无色 + 3 有色。是否有流派需要偏离这个公式？
3. **减费下限**：无色最低锁 1（推荐），还是允许到 0 但给 0 费卡加每回合打出次数限制？
4. **无色能量池的命名**：文档中卡牌费用曾叫"灵气消耗"，与悟道资源"灵气"撞名。战斗 UI 里能量池需要一个不与"灵气"混淆的名字（候选：灵力值/法力/真元）。
5. **中立卡（无 genre）是否永远全无色**：推荐是——中立卡是所有人的公平工具，无色化保证任何构筑都能用。
6. **符箓的新定位**：原卖点"消除流派需求"已随流派限制取消而失效，候选方向：突破境界限制使用 / 纯消耗品复制。可延后决定。
