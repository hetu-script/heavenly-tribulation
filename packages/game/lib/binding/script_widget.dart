import 'package:flutter/material.dart';

class ScriptWidget extends StatelessWidget {
  const ScriptWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
