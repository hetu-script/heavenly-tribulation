import 'package:flutter/material.dart';

class ResponsiveWindow extends StatelessWidget {
  const ResponsiveWindow({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
