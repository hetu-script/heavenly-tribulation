import 'game.ht'

fun getEntityById(id: str) {
  return game.entities[id]
}

fun loadEntity(ent) {
  game.entities[ent.id] = ent
}

fun loadEntities(ents: List) {
  if (config.debug) {
    print('河图: 载入脚本对象 ...')
  }
  for (final ent in ents) {
    game.entities[ent.id] = ent
  }
}