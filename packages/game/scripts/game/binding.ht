import 'game.ht'

fun getBindingById(id: str) {
  return bindings[id]
}

fun loadBinding(bd) {
  bindings[bd.id] = bd
}

fun loadBindings(bds: List) {
  for (final bd in bds) {
    bindings[bd.id] = bd
  }
}
