
fun maxPopulationOfLocationDevelopment(n: int) {
  return (n - 1) * (n - 1) * (n - 1) * 50 + 100
}

fun maxNumberOfWildCharacterPerDevelopment(n: int) {
  return n * n - n * 2 + 2
}

for (var i in range(19)) {
  print(i, maxPopulationOfLocationDevelopment(i))
}
