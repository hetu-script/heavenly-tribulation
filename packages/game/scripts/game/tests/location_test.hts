
fun maxPopulationOfLocationDevelopment(n: int) {
  return (n - 1) * (n - 1) * (n - 1) * 100 + 50
}

fun maxNumberOfCharacterRecruitablePerDevelopment(n: int) {
  assert(n > 0)
  return n < 6 ? n + 2 : 8
}

for (var i in range(20, 2)) {
  // print(maxNumberOfCharacterRecruitablePerDevelopment(i))
}

// 组织中每个等级的人数上限
// 数字越大，等级越低，0是掌门
fun maxMemberNumberOfOrganizationRank(n: int) {
  return n * n - n * 2 + 2
}

// 组织发展度，等于组织可以拥有的境界上限
// 至少是 1，表示可以拥有 筑基 1 人，和 炼气 4 人
fun maxMemberNumberOfOrganizationDevelopment(n: int) {
  assert(n > 0)
  var number = 0
  for (var i in range(n + 1, 1)) {
    number += maxMemberNumberOfOrganizationRank(i)
  }
  return number
}

for (var i in range(13, 1)) {
  print(maxMemberNumberOfOrganizationRank(i))
}