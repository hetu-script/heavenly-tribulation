@lib/scene/caltivation/caltivation.dart 这是一个用于角色升级，天赋树，聚灵阵和打坐的共用场景。但目前我们只针对天赋树部分代码进行重构。

天赋树节点的词条定义在： @assets/data/passives.json5 （和装备词条共用），天赋树的节点形状则定义在 @assets/data/passive_skills.json5 里面。

玩家从最小的圆的五个节点出发（代表游戏中的五种流派），然后通过连接线进入下一个节点，直到最外层的节点。

每个节点可能有也可能没有挂载词条，词条定义在 @assets/data/passives.json5 中。这些词条和另外的装备系统的词条共享。

没有挂载词条的节点则会提供基础属性加成，玩家可以从五种属性中选择一个。

关于修炼，文档在 @docs/docs/how2play/rpg/cultivation/ 目录中，但目前文档比较不全，也可能和实际实现不一致。

每种流派会提供四种不同的构筑思路，分别从境界节点所在的主线上，向左或者向右分支。同时相邻的流派会共享这个构筑思路。

按照顺时针，分别是：御剑，悟道，炼魂，法身，锻体。

玩家可以自由决定怎么加点。但NPC其实是从一个固定的加点路线随机选择的。定义在 @lib/data/common.dart 中的 kCultivationRankPaths 里面。由 \_characterAllocateSkills 调用并自动分配。

之前我已经实现了大部分基础功能，也初步设计了一些节点，跑通了以上设计流程和系统框架。

目前我正在分配天赋树节点功能和连线，因此天赋树被我重置到了没有词条的状态（除了某些境界节点），而且之前写好的预分配天赋的相关代码也暂时注释掉了。

---

目前我们初步有以下需求：

天赋树最开始的 track_0_0 到 track_0_4 一共五个节点，只有在角色尚未分配任何节点时才可以分配。如果角色选择了五个流派天赋树起点之一，之后就不能再选择其他流派的起点。但如果角色取消了全部节点，重新分配，也可以重新选择其他流派入口。此判断只加在caltivation场景中。只对玩家角色生效，npc，以及底层函数调用不做此判断。

另外，我还想实现真正的节点连通判断，也就是说，如果一个节点取消之后，会导致已经加过的点不连通，就不能允许取消。对此是否我们要补充完整的connectedNodes？目前connectedNodes仅仅用于画线，所以只写了单向连通。

请分析现有数据，代码和文档，给出分析。并写一个计划。

---

我计划在天赋树加入更多可以影响卡组构筑的流派关键节点。同时修改原本的境界节点的定位，比如悟道。我的计划如下：@plan/spellcraft.md

目前只是规划阶段，只需要理解现有系统，不要修改任何文件。如果在这个过程中你发现了某些错误，可以顺便暂时记录下来。
