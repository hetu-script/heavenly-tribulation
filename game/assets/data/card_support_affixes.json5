{
  extra_damage_after_attack: {
    id: "extra_damage_after_attack",
    // 不同的词条可能有相同的affixId，非null的affixId只会在一张卡牌上出现一次。
    affixId: "extra_damage_after_attack",
    genre: ["general"],
    // 卡牌类型分为攻击（attack），加持（buff）和特殊（special）三种
    // 额外词条有可能出现在多种类型的卡牌上
    category: ["attack"],
    tags: ["after_attack"],
    script: "extra_damage_after_attack",
    // 实际的数值会在游戏中实际创建时才添加到这里
    // level: 0,
    // values: [0]
    value: [
      {
        // 随机数位于0-1之间，实际值=min+(max-min)*random+level*increment
        random: 0,
        min: 3,
        max: 8,
        increment: 1,
      },
    ],
  },
  defend: {
    id: "defend",
    // 不同的词条可能有相同的affixId，非null的affixId只会在一张卡牌上出现一次。
    affixId: "defend",
    genre: ["general"],
    // 卡牌类型分为攻击（attack），加持（buff）和特殊（special）三种
    // 额外词条有可能出现在多种类型的卡牌上
    category: ["buff", "attack"],
    tags: ["defend"],
    script: "defend",
    // 实际的数值会在游戏中实际创建时才添加到这里
    // level: 0,
    // values: [0]
    value: [
      {
        // 随机数位于0-1之间，实际值=min+(max-min)*random+level*increment
        random: 0,
        min: 3,
        max: 8,
        increment: 1,
      },
    ],
  },
  speed: {
    id: "speed",
    // 不同的词条可能有相同的affixId，非null的affixId只会在一张卡牌上出现一次。
    affixId: "speed",
    genre: ["general"],
    // 卡牌类型分为攻击（attack），加持（buff）和特殊（special）三种
    // 额外词条有可能出现在多种类型的卡牌上
    category: ["buff", "attack"],
    tags: ["speed"],
    script: "speed",
    // 实际的数值会在游戏中实际创建时才添加到这里
    // level: 0,
    // values: [0]
    value: [
      {
        // 随机数位于0-1之间，实际值=min+(max-min)*random+level*increment
        random: 0,
        min: 1,
        max: 4,
        increment: 0.5,
      },
    ],
  },
  dodge: {
    id: "dodge",
    // 不同的词条可能有相同的affixId，非null的affixId只会在一张卡牌上出现一次。
    affixId: "dodge",
    genre: ["general"],
    // 卡牌类型分为攻击（attack），加持（buff）和特殊（special）三种
    // 额外词条有可能出现在多种类型的卡牌上
    category: ["buff", "attack"],
    tags: ["dodge"],
    script: "dodge",
    // 实际的数值会在游戏中实际创建时才添加到这里
    // level: 0,
    // values: [0]
    value: [
      {
        // 随机数位于0-1之间，实际值=min+(max-min)*random+level*increment
        random: 0,
        min: 1,
        max: 4,
        increment: 0.5,
      },
    ],
  },
}
