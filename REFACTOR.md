# 战斗系统重构计划 · 总览

> 本文档是重构的总纲。具体执行步骤拆分为五个阶段文件：
>
> - `REFACTOR_1_FOUNDATION.md` — 地基：伤害结算顺序与既有 bug
> - `REFACTOR_2_DEFENSE.md` — 防御统一与真气穿透
> - `REFACTOR_3_CRIT.md` — 暴击系统与豪气/正气改造
> - `REFACTOR_4_ELEMENTS.md` — 元素体系拆分
> - `REFACTOR_5_PSYCHIC.md` — 精神对抗与敌人数据

## 背景

当前战斗系统"复杂但缺乏深度"：复杂在维度多（4 种防御 × 4 种抗性 × 4 种弱点 × 4 种易伤），
简单在它们之间只是名字不同的换皮——`status_script.ht` 中四种防御的脚本逻辑逐字相同。
深度不来自词条种类，而来自词条之间的联动。本次重构在不推翻既有框架
（卡牌词缀、打造、装备、天赋盘、阴阳气资源、伤势）的前提下，重做伤害类型与对策的映射关系。

## 最终设计：伤害类型矩阵

| 伤害类型               | 对策                          | 机制性质                      | 独占身份                   |
| ---------------------- | ----------------------------- | ----------------------------- | -------------------------- |
| 物理（含风、土系法术） | 护甲（数值，回合减半）        | 数值抵消，被单段重击/穿透针对 | **暴击**（唯一可暴击类型） |
| 真气                   | 护甲（自带 50% 穿透）         | 半穿甲                        | 稳定：无暴击但不被全挡     |
| 火 / 冰 / 雷 / 毒      | 各自的元素抗性（%，上限 75%） | 针对性配装                    | 无视护甲 + 各自元素状态    |
| 精神                   | 对手念力（差值公式，可为 0）  | 属性对抗                      | 无视护甲和抗性             |
| 纯粹                   | 无                            | 稀有                          | 全部无视                   |

元素映射：火→火、水→冰、雷→雷、木→毒、土→物理、风→物理。
（原 `elemental` 伤害类型消失，拆为 fire / ice / lightning / poison 四种独立伤害类型。）

## 已确认的决策

1. **物理 ↔ 护甲**：物理伤害只被护甲（数值）抵消；删除物理抗性。
   **护甲结算顺序移到所有乘区之后**（杀戮尖塔式：格挡最后结算），见阶段 1。
2. **真气自带 50% 穿透**：穿透属性只作用于物理/真气两种伤害；真气+穿透词条有叠加封顶。
   **穿透合并为单一属性**，与伤害类型绑定（不再按攻击类型分型），词条文本注明"只作用于物理和真气伤害"。
3. **只有物理伤害可以暴击**。暴击为纯进攻属性，不设置"抗暴"防御词条。
4. **元素抗性拆分**：删除 physical/chi/elemental/psychic 四抗性，改为 fire/ice/lightning/poison 四抗性。
5. **风、土系法术伤害类型改为物理**（仍为法术卡牌类型，仍受灵气/灵力加成——资源按卡牌类型而非伤害类型加成，现有代码天然支持）。
6. **精神伤害 = 念力对抗**，公式定案（实机测试后调参）：
   `倍率 = clamp((2×自身念力 − 对手念力) / (2×对手念力), 0, 1.5)`
   （≤对手一半 → 0；持平 → 50%；≥2倍 → 150% 封顶。）
7. **豪气改为暴击资源**：`energy_positive_pure` 改名 **`energy_positive_crit`**，消耗 1 层 → 下一次物理攻击必定暴击。
8. **正气改为穿透资源**：`energy_positive_leech` 改名 **`energy_positive_penetrate`**，消耗 1 层 → 本回合获得 20% 防御穿透。
   **吸血（leech）机制整体删除**（与仙侠主题不符）；戾气保留原效果（攻击后自身受伤），
   id 随命名对称规则改为 **`energy_negative_penetrate`**（阴气与阳气英文名仅以 positive/negative 区分；
   风味上可理解为"穿透的是自身"）。
9. **护盾四合一并并入阴阳气**：浩然之气 = **`energy_positive_shield`**，每层抵挡一次任意非纯粹伤害，触发消耗 1 层；
   其对位萧索之气 = **`energy_negative_shield`**：持有者下一次受到的任意类型伤害翻倍（含纯粹），触发消耗 1 层。
   衰气（`energy_negative_pure` → 改名 **`energy_negative_crit`**）：降低下一次暴击的伤害（触发时消耗）。
   至此阴气阳气仅清气/浊气一对留空。
10. **伤势系统去掉中毒**：伤势只含流血（物理）、内伤（真气）、幻觉（精神），规则保持"不可减免"。
    中毒迁入元素持续伤害（见决策 11，吃毒抗）。
11. **四种元素持续伤害（DOT）统一规则**：回合开始时每层失去 X 点生命（伤害类型为对应元素，受对应抗性减免），
    回合结束时移除 Y 层。同一规则模板 + 四种参数制造节奏差异（详见 `REFACTOR_4_ELEMENTS.md`）：
    灼伤 `element_dot_fire`（5 / 减 1 层，稳定）、触电 `element_dot_lightning`（8 / 全部衰减，爆发）、
    冻伤 `element_dot_ice`（3 / 每 2 回合减 1 层，绵长）、中毒 `element_dot_poison`（2 / 不衰减，治疗可驱散，累积）。
    id 统一为 `element_dot_fire/ice/lightning/poison`，共用同一参数化脚本，便于检索。
    **【参数均为初值，实机调；雷（全衰减）与毒（不衰减）是两个极端，重点验证】**
12. **易伤规则**：触发对应伤害后自动消耗即可，回合开始不再衰减。（代码现状已是如此，只需改文档。）
13. **幻觉 bug**（debuff 误加给 opponent）：已由作者直接修复，不在计划内。
14. **剑气溢出 debuff 池（`kDebuffs`）扩充**：只加入易伤与四种元素持续伤害（阶段 2 易伤、阶段 4 元素 DOT）。
    **阴气不入池**——阴气本身设计上也是一种资源（未来会有消耗阴气打出的卡牌），
    若能通过剑气溢出轻松获得，溢出惩罚会变成刷资源的奖励，两个系统同时失效。
15. **弱点（百分比）只保留四种元素分型**（`weakness_fire/ice/lightning/poison`，即负元素抗性）；
    **易伤（数值）合并为不分类型的单一 `vulnerable`**，保留在 `kDebuffs` 随机池中。
16. **删除物理/真气/精神抗性（暗黑2式）**：物理只对护甲、真气只对护甲（50% 穿透）、精神只对念力，
    每种伤害恰好一个对策轴，不保留恐怖黎明式的多重抗性叠加。
    原有的 `physicalResist/chiResist/psychicResist` 装备词条与天赋节点在阶段 4 全部移除或替换。

## 阶段划分（每阶段结束游戏可运行、可验证）

| 阶段 | 内容                                                                | 性质        | 依赖   |
| ---- | ------------------------------------------------------------------- | ----------- | ------ |
| 1    | 防御四合一为护甲；真气 50% 穿透；穿透/护盾/易伤收敛                 | 数据为主    |        |
| 2    | 暴击系统全栈新增；豪气→暴击、正气→穿透、衰气→暴击削弱               | 代码+数据   | 阶段1  |
| 3    | 元素拆分（风土→物理、水→冰、木→毒）；四元素抗性；降抗词条；元素状态 | 数据+新内容 | 阶段 1 |
| 4    | 精神念力对抗公式；敌人念力/抗性谱；UI 展示                          | 代码+内容   | 阶段 3 |

阶段 2、3、4 之间无强依赖，可根据内容产出（尤其是美术）灵活排序。

## 全局约定

- 每个阶段完成后：运行 VSCode 任务 `compileAllGameScripts`（或 `python build.py`）编译脚本，
  `flutter analyze` 无新增错误，并进行至少一场玩家 vs NPC 的实机战斗冒烟测试。
- 所有被删除的本地化键必须同步从 `assets/locale/zh/` 移除；新增键同步补全。
- 每个阶段文件末尾列出本阶段涉及的**文件清单**与**验证清单**。
- 文档（`docs/docs/how2play/rpg/battle/`）在对应阶段结束时同步更新，
  `resource/readme.md` 在阶段 1 重写为只描述现实（未实现的提案移入设计日记或删除）。
- 既有 bug 与历史矛盾已全部分配到各阶段：`self_attacking`/`self_buffing` 死回调修复、resource/readme 重写（阶段 1），
  `kDebuffs` 扩充（决策 14，阶段 2/3/4）。
