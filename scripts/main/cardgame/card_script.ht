// import 'battle_character.ht'

/// 卡牌的每个词条都对应一个异步async脚本。
/// 这个脚本将会在这张牌被打出时invoke

namespace CardScript {

  /// ------------------------------------------------------------
  /// 绝世词条
  /// ------------------------------------------------------------
  /// 抽牌

  function draw_cards(self, opponent, affix, mainAffix) {
    self.drawCards(affix.value[0])
  }
  
  /// ------------------------------------------------------------
  /// 主词条
  /// ------------------------------------------------------------
  /// 攻击
  function attack(self, opponent, affix, mainAffix) {
    opponent.takeDamage({
      isMain: true,
      kind: affix.kind,
      cardType: affix.cardType,
      damageType: affix.damageType,
      baseValue: affix.value[0],
    })
  }
  /// 多次攻击
  function attack_multiple(self, opponent, affix, mainAffix) {
    for (final i in range(affix.value[0])) {
      opponent.takeDamage({
        isMain: true,
        kind: affix.kind,
        cardType: affix.cardType,
        damageType: affix.damageType,
        baseValue: affix.value[1],
      })
    }
  }
  /// 攻击附带施加减益
  function attack_debuff(self, opponent, affix, mainAffix) {
    opponent.takeDamage({
      isMain: true,
      kind: affix.kind,
      cardType: affix.cardType,
      damageType: affix.damageType,
      baseValue: affix.value[0],
    })
    opponent.addStatusEffect(affix.buffId, amount: affix.value[1])
  }

  /// 基础拳法（默认卡）：固定造成 角色境界 × 5 的伤害
  /// 数值读取角色境界而非卡牌境界，无法用 valueData 表达，故用脚本
  function attack_rank_scaled(self, opponent, affix, mainAffix) {
    opponent.takeDamage({
      isMain: true,
      kind: affix.kind,
      cardType: affix.cardType,
      damageType: affix.damageType,
      baseValue: self.data.rank * 5,
    })
  }

  /// 获得速度和防御
  async function speed_quick_defend(self, opponent, affix, mainAffix) {
    self.addStatusEffect('speed_quick', amount: affix.value[0])
    self.addStatusEffect('defense', amount: affix.value[1])
  }
  /// 获得闪避和防御
  async function dodge_nimble_defend(self, opponent, affix, mainAffix) {
    self.addStatusEffect('dodge_nimble', amount: affix.value[0])
    self.addStatusEffect('defense', amount: affix.value[1])
  }

  /// 提升生命上限
  async function buff_lifemax(self, opponent, affix, mainAffix) {
    self.setLifeMax(self.lifeMax + affix.value[0], rejuvenate: true)
  }
  /// 降低对手生命上限
  async function debuff_lifemax(self, opponent, affix, mainAffix) {
    opponent.setLifeMax(opponent.lifeMax - affix.value[0])
  }

  /// 加持

  /// 获得生命
  async function heal(self, opponent, affix, mainAffix) {
    await self.changeLife(affix.value[0], isHeal: true)
  }
  /// 回复生命上限百分比的生命，可以超过上限
  async function heal_lifeMax(self, opponent, affix, mainAffix) {
    final regen = (affix.value[0] / 100 * self.lifeMax).round()
    self.changeLife(regen, isHeal: true)
  }
  
  /// 获得增益
  async function self_buff(self, opponent, affix, mainAffix) {
    self.addStatusEffect(affix.buffId, amount: affix.value[0])
  }
  
  /// 对手获得减益
  async function opponent_debuff(self, opponent, affix, mainAffix) {
    opponent.addStatusEffect(affix.buffId, amount: affix.value[0])
  }

  /// 降低对手全部元素抗性（以弱点形式附加，与抗性互相抵消）
  async function reduce_resist_all(self, opponent, affix, mainAffix) {
    for (var elementResist in ['fire', 'ice', 'lightning', 'poison']) {
      opponent.addStatusEffect('weakness_${elementResist}', amount: affix.value[0])
    }
  }

  /// 攻击额外词条

  /// 攻击后每造成10点伤害回复生命
  async function by_damage_heal(self, opponent, affix, mainAffix) {
    final cardDamage = self.cardFlags.damage.total ?? 0
    if (cardDamage < 10) return
    final amount = (cardDamage / 10).floor()
    self.changeLife(affix.value[0] * amount)
  }

  /// 攻击后每造成10点伤害获得防御
  async function by_damage_defend(self, opponent, affix, mainAffix) {
    final cardDamage = self.cardFlags.damage.total ?? 0
    if (cardDamage < 10) return
    final amount = (cardDamage / 10).floor()
    self.addStatusEffect('defense', amount: affix.value[0] * amount)
  }

  /// 根据属性提升伤害
  async function for_attribute_increase_damage(self, opponent, affix, mainAffix) {
    final amount = self.data.stats[affix.attributeId] * 0.005
    self.cardFlags.damage.percentageChange1 += amount
  }

}
