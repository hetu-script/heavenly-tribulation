
/// 任务线
/// 当progress达到100时，任务完成
struct TaskLine {
  constructor ({
    atCity,
    itemId,
    target,
    amount,
  }) {
    this.id = crypto.randomUID(withTime: true)
    this.itemId = itemId
    this.amount = 0
  }
}
